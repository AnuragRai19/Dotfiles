background {
    monitor =
    path = /home/anuragrai/Pictures/wallpapers/GokuMUI.jpg
    color = rgba(40, 42, 54, 1.0)  # Dracula background
    blur_passes = 2
}

image {
    monitor =
    path = /home/anuragrai/Pictures/wallpapers/GokuBlack.jpg
    size = 100
    rounding = 50  # circle

    border_size = 3
    border_color = rgba(189, 147, 249, 0.8)  # Dracula purple

    position = 0, 80
    halign = center
    valign = center
}

input-field {
    monitor =
    size = 22%, 6%
    outline_thickness = 2

    inner_color = rgba(68, 71, 90, 0.8)  # Dracula selection bg

    outer_color = rgba(189, 147, 249, 0.9) rgba(80, 250, 123, 0.9) 135deg  # purple to green
    check_color = rgba(80, 250, 123, 0.9) rgba(139, 233, 253, 0.9) 120deg  # green to cyan
    fail_color = rgba(255, 85, 85, 0.9) rgba(255, 121, 198, 0.9) 60deg     # red to pink

    font_color = rgba(248, 248, 242, 1.0)  # Dracula foreground
    fade_on_empty = true

    rounding = 20

    position = 0, -180
    halign = center
    valign = center
}

label {
    monitor =
    text =   Hi there, Anurag Rai
    color = rgba(189, 147, 249, 1.0)  # Dracula purple
    font_size = 26
    font_family = JetBrainsMono Nerd Font

    position = 0, -60
    halign = center
    valign = center
}
